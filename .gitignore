# APR runtime
.apr/
.apr/rounds/
round_*.fallback.md

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Temporary
*.tmp
*.temp
apr.funcs
test_regex.sh

# Release artifacts (generated by CI)
*.sha256
checksums.txt
# ...except the checksums install.sh / `apr update` fetch to verify what they
# download. These must be committed or verification silently degrades to "skip".
!apr.sha256
!install.sh.sha256
!gapr.sha256
!gemini-helper.sh.sha256
!CHECKSUMS.sha256
!lib/*.sha256

# bv (beads viewer) local config and caches
.bv/

# Test artifacts (ephemeral logs)
tests/logs/

# Node.js
node_modules/
*.log
